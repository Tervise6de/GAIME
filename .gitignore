# OS/editor junk — extend with technology-specific rules once a stack is chosen
.DS_Store
Thumbs.db
*.swp

# dependencies & local artifacts
node_modules/

# GIF working frames (tools/make_gif.mjs) — assembled output is committed, frames are not
media/gif_frames/
